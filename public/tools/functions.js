//MODAL PARA ELIMINAR USURIO
// alert('Hola');
function tramite_date(url=''){
    // alert(url);
    var path_delete = url;
    var elemento = document.getElementById('delete');
    elemento.setAttribute('href', path_delete);
}
